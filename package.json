{
  "name": "urscore-ai-workspace",
  "version": "1.0.0",
  "description": "Monorepo workspace for UrScore AI developer intelligence platform",
  "scripts": {
    "bootstrap": "npm run install-backend && npm run install-worker && npm run install-frontend",
    "install-backend": "npm --prefix backend install",
    "install-worker": "npm --prefix worker install",
    "install-frontend": "npm --prefix frontend install",
    "dev-backend": "npm --prefix backend run dev",
    "dev-frontend": "npm --prefix frontend run dev",
    "dev": "npx concurrently -k -n \"backend,frontend\" -c \"magenta,cyan\" \"npm run dev-backend\" \"npm run dev-frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
